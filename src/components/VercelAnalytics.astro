---
// Vercel Web Analytics — privacy-first, no consent required.
// Loaded unconditionally; does not conflict with ConsentBanner.
---
<script>
  import { inject } from '@vercel/analytics';
  inject();
</script>
